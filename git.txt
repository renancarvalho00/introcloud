git init

git config --global user.name "renancarvalho00"
git config --global user.email carvalhorenan00@gmail.com

git add .

git commit -m "Primeiro commit"

git remote add origin https://github.com/renancarvalho00/introcloud.git

git pull origin master

git push origin master